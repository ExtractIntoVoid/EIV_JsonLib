name: Publish Packages

on:
  push:
    tags:
      - "release-*"
  workflow_dispatch:
    # allow manual trigger

jobs:
  publish:
    permissions:
      contents: write
      packages: write
    uses: ExtractIntoVoid/workflows/.github/workflows/package.yml@main
    with:
      ProjName: EIV_JsonLib
    secrets:
      TOKEN: ${{ secrets.GITHUB_TOKEN }}
